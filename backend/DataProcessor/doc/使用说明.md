# DataProcessor 使用说明

## 数据爬取

### 1. 配置 GitHub Token
                                                                                                                                                        `   
在 `DataProcessor` 目录下创建 `.env` 文件：

```
GITHUB_TOKEN=your_github_token_here
```

### 2. 运行爬虫

```bash
cd DataProcessor
python github_text_crawler.py
```

按提示输入仓库名（如 `apache/echarts`）和爬取数量。

### 3. 处理数据

```bash
python data_processor.py <爬取生成的json文件>
```

将生成处理后的时序数据和文本数据。

---

## 🚀 DataPulse 可视化平台

爬取的数据可以使用 **DataPulse** 平台进行可视化分析。

### 启动后端

```bash
cd ../Datapulse/backend
pip install -r requirements.txt
python app.py
```

### 启动前端

```bash
cd ../Datapulse/frontend
npm install
npm run dev
```

访问 http://localhost:3000 查看可视化界面。

### 功能说明

1. **时序分析** - 查看 Star/Commit/PR/Contributor 的月度变化趋势
2. **Issue 分析** - 按月查看 Issue 分类、关键词
3. **波动归因** - 自动识别显著波动并关联 Issue 文本进行解释

